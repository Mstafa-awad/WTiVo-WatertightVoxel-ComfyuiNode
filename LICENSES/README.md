# License files

The root `LICENSE` is the license for the combined open-source WTiVo distribution: **GPL-3.0-or-later**.

Files with `SPDX-License-Identifier: Apache-2.0` preserve Apache licensing/attribution from the CelloCut/FaithC-derived code lineage. The named copies here make the dependency map easier to audit; identical standard license texts are intentionally duplicated under component names.

For dependencies that are downloaded rather than vendored (PyTorch, NumPy, Trimesh, vcpkg ports, CUDA), the exact license files accompanying the installed/downloaded version remain authoritative. See `../THIRD_PARTY_NOTICES.md`.
